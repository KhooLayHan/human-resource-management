package org.bhel.hrm.server.daos;

import org.bhel.hrm.server.domain.TrainingCourse;
import org.bhel.hrm.server.domain.User;

import java.util.Optional;

/**
 * Data Access Object interface for TrainingCourse entities.
 * Manages the catalog of available training courses.
 */
public interface TrainingCourseDAO extends DAO<TrainingCourse, Integer> {

//    Optional<User> findByUsername(String username);

}
